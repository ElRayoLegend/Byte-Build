Creamos el JPA el cual contendra 
EntityManagerFactory emf = buildEntityManagerFactory();
El entityManager que nos sirve para conectarnos con el 
Hibernate y la persistencia, que a la vez crea la persistencia
del EntityManager


Cree el modelo de la entidad Compra con las respectivas 
variables encapsuladas, setters and getters, los constructores
y el toString. 